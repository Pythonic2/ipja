version: '3.8'
services:
  questionario:
    build:
      context: .
      dockerfile: Dockerfile
    pull_policy: build
    restart: always
    ports:
      - 8004:8004
    env_file:
      - stack.env
    networks:
      - pi_default
    volumes:
      - ipja_db:/var/lib/postgresql/data
      - ipja_app:/app/

volumes:
  ipja_db:
  ipja_app:

networks:
  pi_default:
    external: true
